package enterprises.orbital.evekit.model;

/**
 * A marker interface for things stored in the model cache.  Sync strategies should implement this
 * interface with a class containing the fields appropriate for the data being cached.
 */
public interface ModelCacheData {
}
